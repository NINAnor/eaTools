#' Example raster data for eaTools
#'
#' This package contains example spatial data. accounting_area is the outline of five regions, making up the accounting area. ex_polygons is a sf object with a dummy condition variable (un-scaled). It does not cover the entire accounting area. ex_raster is a 1000x1000m raster grid over the extent of the account area, with cells randomly classed as either 1, 2 or 3.
#'
#' @format A stars object with 40 rows and 34 column(s)
#'
"ex_raster"
